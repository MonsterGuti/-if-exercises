total_minutes = int(input("Enter the minutes in total: "))

days = total_minutes // (24 * 60)
hours = (total_minutes % (24 * 60)) // 60
minutes = total_minutes % 60

print("")
print(f"The time calculated in days, hours, and minutes is {days} days, {hours} hours, and {minutes} minutes.")
